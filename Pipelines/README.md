This repository demonstrates a simple CI using Github Actions 

For every new code pushed to main branch would trigger this pipeline.

The Github Actions would then check for code, Install Node.js, Install dependencies, Build the project and Run all testes
